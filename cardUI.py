
def LoadCardUI(stdscr):
    # Should be the screen where the deck is displayer with the name of the card
    event = stdscr.getch()
    if(event == ord('a')):
        pass
    elif(event == ord('d')):
        pass
